using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using Highscore;

public class HighScorePrefab : MonoBehaviour {

	//public GameObject name;
	//public GameObject score;
	//public GameObject rank;

	//public void SetScore (string name, string score, string rank) {

		//this.name.GetComponent<Text> ().text = name;
		//this.score.GetComponent<Text> ().text = score;
		//this.rank.GetComponent<Text> ().text = rank;

		//this.name.GetComponent<Text> ().text += "\n#" + (i + 1);
		//this.score.GetComponent<Text> ().text += "\n" + tmpScore.Username;
		//this.rank.GetComponent<Text> ().text += "\n" + tmpScore.Score;

		//tmpObjec.GetComponent<RankText> ().text += "\n#" + (i + 1);
		//tmpObjec.GetComponent<NameText> ().text += "\n" + tmpScore.Username;
		//tmpObjec.GetComponent<ScoreText> ().text += "\n" + tmpScore.Score;
		//scoreParent = GameObject.Find ("Scores").transform;
		
	//}
}
